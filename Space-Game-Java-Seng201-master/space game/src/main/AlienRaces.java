package main;
/**
 * 
 * @author mkw60
 *Holds the Enum Variables this is needed to be in its own Class so it can
 * be used throughout the Project.
 */


/**
 * Holds the enum values used in AlienRaves.
 */
public enum AlienRaces {
ROBOT, SLUG, MANTIS, ROCK, GREENMAN, HUMAN
}
